#rubygem
  %h1&= @rubygem.name
  = link_to('Rubygems.org Page', "https://rubygems.org/gems/#{@rubygem.name}")
  %hr
  %h3 It does what now?
  &= @rubygem.info
  %h3 Latest Version
  &= @rubygem.latest_version
  #stats
    - gem_stats(@rubygem).each do |s,v|
      %h1&= s
      = v